# Data Integrity Rules

Cache all external API responses with retrieval timestamps. Validate all extracted issue claims with the schema before storing. Do not overwrite raw evidence when refreshing; keep content hashes and retrieval history where practical.
