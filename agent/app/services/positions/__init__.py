"""Candidate-positions cache + research unit (position-search redesign T1+T2).

A pure data/service layer: a Mongo ``candidate_positions`` cache (``schema`` +
``store``) and the reusable ``research_candidate_positions`` pipeline. Nothing in
the app imports this package yet.
"""
